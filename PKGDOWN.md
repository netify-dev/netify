# Instructions for deploying pkgdown website

The website for `netify` uses the `pkgdown` package. The full manual for `pkgdown` is available [here](https://pkgdown.r-lib.org/index.html).

## Preparing to deploy the site
