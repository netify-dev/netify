% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix_utils.R
\name{as.matrix.netify}
\alias{as.matrix.netify}
\title{Coerce a netify object to a plain matrix}
\usage{
\method{as.matrix}{netify}(x, time = NULL, ...)
}
\arguments{
\item{x}{A netify object.}

\item{time}{For longitudinal netify objects, either the integer index
or character label of the time slice to extract. Defaults to the
first slice and emits a hint when used implicitly.}

\item{...}{Additional args (ignored).}
}
\value{
A plain numeric matrix with \code{dim} and \code{dimnames} only (no
\code{netify} class, no netify metadata attributes).
}
\description{
Strips the netify class and netify-specific attributes so the result
is a clean numeric matrix carrying only \code{dim} and \code{dimnames}. For
longitudinal netify objects, \code{time} selects which slice to return;
it defaults to the first time period and emits a hint. Round-trips
(\code{net |> as.matrix() |> netify()}) recover a fresh cross-sectional
netify object, but structural attributes (symmetric / diag_to_NA /
weight) are re-detected from the matrix on the way back in rather
than copied across, so a directed matrix or one with a non-NA
diagonal will be flagged accordingly.
}
\examples{
data(icews)
icews_2010 <- icews[icews$year == 2010, ]
net <- netify(icews_2010, actor1 = "i", actor2 = "j",
    symmetric = FALSE, weight = "verbCoop")
m <- as.matrix(net)
dim(m)
class(m)

}
\seealso{
\code{\link{get_adjacency}} for the data.frame-input
counterpart that also accepts a netify object; \code{\link{netify}}
for rebuilding a netify object from a plain matrix.
}
\author{
Cassy Dorff, Shahryar Minhas
}
