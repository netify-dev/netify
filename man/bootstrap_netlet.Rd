% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_netlet.R
\name{bootstrap_netlet}
\alias{bootstrap_netlet}
\title{Bootstrap any user-supplied function of a netify object}
\usage{
bootstrap_netlet(
  netlet,
  fn,
  n_boot = 200L,
  alpha = 0.05,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{netlet}{A \code{netify} object.}

\item{fn}{Function. Takes a netlet, returns a single numeric value
or a named numeric vector. Called once per bootstrap draw.}

\item{n_boot}{Integer. Number of bootstrap replicates (default
\code{200}).}

\item{alpha}{Numeric in (0, 1). Two-sided percentile-CI alpha
(default \code{0.05} → 95\% intervals).}

\item{seed}{Optional integer. If supplied, sets a local RNG seed
without leaking into the user's global stream.}

\item{verbose}{Logical. If \code{TRUE} (default), print a progress
ticker every 50 draws.}
}
\value{
A \code{data.frame} with one row per element of \code{fn(netlet)}
and columns:
\describe{
\item{\code{metric}}{Name of the output element (or \code{"value"} for
scalar fn).}
\item{\code{point}}{Point estimate from \code{fn(netlet)} on the
original (un-resampled) netlet.}
\item{\code{mean}}{Bootstrap mean.}
\item{\code{sd}}{Bootstrap standard deviation.}
\item{\code{lower}, \code{upper}}{Lower / upper percentile CI bounds.}
}
The full per-draw matrix is stashed as
\code{attr(out, "bootstrap_draws")} for callers who want the empirical
distribution (e.g., for kernel-density plots).
}
\description{
Resamples actors with replacement (Snijders & Borgatti 1999 vertex
bootstrap) per panel, rebuilds the netlet on each draw, applies
the user-supplied \code{fn}, and returns the per-draw values plus
percentile confidence intervals.
}
\details{
This generalizes the per-graph-stat bootstrap that
\code{summary(net, bootstrap = TRUE)} runs internally: any scalar / named
numeric vector summary of a netify can be wrapped. Useful for
homophily / mixing-matrix / centrality stats that don't expose
their own bootstrap, and for downstream fit summaries (e.g., the
coefficient of a \code{to_igraph} -> \verb{igraph::cluster_*} -> modularity
pipeline).

Resampling is \strong{vertex-level}, not edge-level: actors are
sampled with replacement and the netlet's adjacency is sliced
accordingly. For longitudinal netlets, the same resampled index
set is applied to every period (preserves within-actor
dependence). Multilayer netlets are unsupported — bootstrap each
layer independently via \code{subset_netify(layers = ...)} first.
}
\section{Parallel execution}{

\code{bootstrap_netlet} runs serially. When \code{n_boot > 50} and the netlet
is large enough that each draw is non-trivial (rule of thumb:
N > ~1000 with the default centrality-heavy \code{fn}), parallelism
helps. There is no built-in \verb{parallel =} argument; instead drive
the loop yourself with \code{future.apply::future_lapply()}, which
respects whatever \code{future::plan()} the caller set:
\preformatted{
library(future); library(future.apply)
plan(multisession)        # or multicore on Linux/macOS
draws <- future_lapply(1:n_boot, function(b) {
  idx <- sample(actors, length(actors), replace = TRUE)
  fn(subset_netify(net, actors = idx))
}, future.seed = TRUE)
}
Then summarize \code{draws} exactly as \code{bootstrap_netlet} does
internally. Wrapping the inner body of \code{bootstrap_netlet} in a
future-aware loop is a small refactor and is the recommended path
for ~15K-node weekly snapshots where the serial pass would tie up
a single core for hours.
}

\examples{
\dontrun{
data(icews)
net <- netify(icews[icews$year == 2010, ],
actor1 = "i", actor2 = "j", symmetric = FALSE, weight = "verbCoop")
# bootstrap a custom scalar: mean(closeness)
my_stat <- function(net) {
sa <- summary_actor(net, stats = "closeness")
c(mean_closeness = mean(sa$closeness_all, na.rm = TRUE))
}
boot_out <- bootstrap_netlet(net, fn = my_stat, n_boot = 100, seed = 1)
boot_out
}

}
\author{
Cassy Dorff, Shahryar Minhas
}
