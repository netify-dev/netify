% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_layout_attribs.R
\name{merge_layout_attribs}
\alias{merge_layout_attribs}
\title{Merge netlet attributes with layout information}
\usage{
merge_layout_attribs(netlet, nodes_list, edges_list)
}
\arguments{
\item{netlet}{A \code{netify} object containing the network data.}

\item{nodes_list}{A list of data frames (or a single data frame for cross-sectional data) containing
node positions with columns \code{actor}, \code{x}, and \code{y}. For longitudinal data, each list element
corresponds to a different time slice.}

\item{edges_list}{A list of data frames (or a single data frame for cross-sectional data) detailing
the connections between nodes. Each data frame should include at least the columns \code{from}, \code{to},
and for longitudinal data, \code{time}.}
}
\value{
A list containing two data frames: \code{nodal_data} and \code{edge_data}. Each data frame incorporates
the corresponding node or edge data along with their spatial coordinates (and time indices for
longitudinal data).
}
\description{
This function organizes \code{netlet} data by integrating x and y positions from \code{nodes_list} and
connection data from \code{edges_list} into the node and edge data structures returned by
\code{decompose_netlet}. It supports both cross-sectional and longitudinal data formats.
}
